{
  "name": "lambda",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "cdk"
  ],
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-lambda": "^3.700.0",
    "@aws-sdk/client-scheduler": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "cheerio": "^0.22.0",
    "dotaconstants": "^10.8.0",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "decrypt": "sops -d environment.js.enc > environment.js",
    "encrypt": "sops -e environment.js > environment.js.enc",
    "deploy": "npm run decrypt && npm --workspace c0rbot-cdk exec cdk deploy",
    "synth": "npm run decrypt && npm --workspace c0rbot-cdk exec cdk synth"
  },
  "author": "",
  "license": "ISC"
}
