# Ignore versions published in the last week, so a compromised release has time to be
# caught and unpublished before a build here can pick it up. The unit is days.
# Needs npm >= 11.10.0 (Node 24 ships it) and is silently ignored below that.
min-release-age=7
